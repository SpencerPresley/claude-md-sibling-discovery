{
  "name": "pyrefly-lsp",
  "description": "Python code intelligence via Pyrefly's language server. Uses deterministic workspace indexing and resolves the project's own interpreter and site-packages, so it needs no venv setup and no project-declared dependency.",
  "version": "0.3.0",
  "author": {
    "name": "Spencer Presley"
  },
  "lspServers": {
    "pyrefly": {
      "command": "uv",
      "args": [
        "run",
        "--no-project",
        "--quiet",
        "${CLAUDE_PLUGIN_ROOT}/server/pyrefly_lsp.py"
      ],
      "extensionToLanguage": {
        ".py": "python",
        ".pyi": "python"
      },
      "startupTimeout": 60000
    }
  }
}
