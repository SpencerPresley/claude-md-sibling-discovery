[project]
name = "claude-md-discovery-extended"
version = "0.4.0"
description = "Extends CLAUDE.md auto-discovery to directories outside the project tree, with content-hash deduplication and change detection"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
